[pytest]
# Async tests carry an explicit `pytestmark = pytest.mark.asyncio`, so strict
# mode is right. Pinning the fixture loop scope silences a deprecation warning
# that would otherwise appear on every run and worry students.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
testpaths = tests
